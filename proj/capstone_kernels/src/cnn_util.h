#ifndef __CNN_UTIL_H
#define __CNN_UTIL_H

// i guess we need these too?
#define MIN(a,b) (((a)<(b))?(a):(b))
#define MAX(a,b) (((a)>(b))?(a):(b))

typedef float cnndata_t;
typedef int   index_t;

#endif
